name: Randomy
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  awesome_dialog: ^3.0.2
  cupertino_icons: ^1.0.4
  flutter_launcher_icons: ^0.11.0
  flutter_riverpod: ^2.1.3
  flex_color_scheme: ^6.1.2
  hive_flutter: ^1.1.0
  go_router: ^6.0.1
  hive_test: ^1.0.1

flutter_icons:
  image_path: "assets/toss.png"
  android: true

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/
